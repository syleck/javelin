#include "../userspace/kernel.h"
#include <stdbool.h>

int main() {
    printf("Shell started. Licensed under the AGPL for the Javelin kernel\n");
    bool state = false;
    while(state == false) {
        
    }
    return 0;
}